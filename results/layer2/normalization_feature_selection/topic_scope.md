# Normalization / Feature Selection Topic Scope - Superseded

This topic is no longer active. It has been split into `Normalization` and `Panel Design`.

No formal rendering should use this superseded package as an active method-selection source.
